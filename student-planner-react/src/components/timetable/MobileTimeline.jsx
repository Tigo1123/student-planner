import ClassBlock from "./ClassBlock.jsx"; import { DAY_LABELS,WEEKDAYS } from "../../utils/scheduleUtils.js";
function MobileTimeline({items,selectedDay,onDay,current,onView,today}){const dayItems=items.filter(i=>i.dayOfWeek===selectedDay).sort((a,b)=>a.startTime.localeCompare(b.startTime));return <section className="mobile-timetable" aria-labelledby="mobile-day-title"><div className="day-tabs" role="tablist" aria-label="Weekdays">{WEEKDAYS.map(day=><button role="tab" aria-selected={day===selectedDay} aria-current={day===today?"date":undefined} key={day} onClick={()=>onDay(day)}>{DAY_LABELS[day].slice(0,2)}<span className="sr-only">{DAY_LABELS[day]}{day===today?", today":""}</span></button>)}</div><h2 id="mobile-day-title">{DAY_LABELS[selectedDay]} {selectedDay===today&&<span>Today</span>}</h2>{dayItems.length?<div className="mobile-day-list">{dayItems.map(item=><div className="mobile-day-entry" key={item.id}><span>{item.startTime}</span><ClassBlock item={item} isCurrent={current?.id===item.id} onView={onView}/></div>)}</div>:<p className="mobile-day-empty">No classes scheduled for this day.</p>}</section>}
export default MobileTimeline;
