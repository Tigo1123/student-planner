import CourseIcon from "../courses/CourseIcon.jsx"; import { formatScheduleRange } from "../../utils/scheduleUtils.js";
function ClassBlock({item,isCurrent,onView,style,compact=false}){return <button className={`class-block${isCurrent?" class-block--current":""}${compact?" class-block--compact":""}`} style={{...style,"--course-color":item.course?.color||"#174f5c"}} type="button" onClick={()=>onView(item)} aria-label={`${item.course?.name}, ${formatScheduleRange(item.startTime,item.endTime)}${item.room?`, room ${item.room}`:""}${isCurrent?", happening now":""}`}><CourseIcon icon={item.course?.icon}/><span><strong>{item.course?.code||item.course?.name}</strong><small>{formatScheduleRange(item.startTime,item.endTime)}</small>{item.room&&<small>{item.room}</small>}</span>{isCurrent&&<b>Now</b>}</button>}
export default ClassBlock;
