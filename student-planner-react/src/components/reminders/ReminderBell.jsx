import{useReminders}from"../../reminders/reminderContext.js";function ReminderBell(){const{due,setPanelOpen}=useReminders();return <button className="reminder-bell" type="button" onClick={()=>setPanelOpen(true)} aria-label={due.length?`${due.length} reminders due`:"No reminders due. Open reminders"}><span aria-hidden="true">♢</span>{due.length>0&&<b>{due.length>99?"99+":due.length}</b>}</button>}export default ReminderBell;
