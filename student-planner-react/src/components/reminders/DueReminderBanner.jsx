import{useReminders}from"../../reminders/reminderContext.js";function DueReminderBanner(){const{due,setPanelOpen}=useReminders();if(!due.length)return null;return <div className="due-reminder-banner" role="status"><span aria-hidden="true">♢</span><p><strong>{due.length} {due.length===1?"reminder needs":"reminders need"} your attention</strong><small>Review what is due without leaving your planner.</small></p><button type="button" onClick={()=>setPanelOpen(true)}>View reminders</button></div>}export default DueReminderBanner;
